var React = require('react');
var ReactDOM = require('react-dom');

class Publish extends React.Component {
  render() {
    return (
      <div>
        <strong>[Publish Modal]</strong>
      </div>
    )
  }
}

module.exports = Publish;
